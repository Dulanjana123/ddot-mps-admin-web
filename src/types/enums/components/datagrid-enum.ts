export enum DataGridDensityScale {
  Compact = "compact",
  Comfortable = "comfortable",
  Standard = "standard",
}

export enum ProcessingMode {
  Client = "client",
  Server = "server",
}

export enum Density {
  Comfortable = "comfortable",
  Compact = "compact",
  Standard = "standard"
}
