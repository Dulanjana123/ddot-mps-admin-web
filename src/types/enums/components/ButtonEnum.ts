export enum ButtonSizes {
   sm = "sm",
   md = "",
   lg = "lg",
}

export enum ButtonTypes {
   reset = "reset",
   submit = "submit",
   button = "button",
}

export enum IconPlacement {
   Right = "right",
   Left = "left",
}
