export enum HorizontalPosition {
  Left = "left",
  Center = "center",
  Right = "right",
}

export enum VerticalPosition {
  Top = "top",
  Bottom = "bottom",
}

export enum ToastVariant {
  Success = "success",
  Error = "error",
  Warning = "warning",
  Info = "info",
}
